import Image from 'next/image';import fs from 'fs';import path from 'path';import type {Artwork as A} from '@/data/site';
export default function Artwork({art,className='',priority=false}:{art:A;className?:string;priority?:boolean}){const exists=fs.existsSync(path.join(process.cwd(),'public',art.src));const ratio=art.ratio==='3:4'?'aspect-[3/4]':art.ratio==='4:5'?'aspect-[4/5]':art.ratio==='4:3'?'aspect-[4/3]':'aspect-video';return <figure className={`relative overflow-hidden border border-line bg-coal ${ratio} ${className}`}>{exists&&!art.src.endsWith('.mp4')?<Image src={art.src} alt={art.alt} fill sizes="(max-width:768px) 100vw, 50vw" className="object-cover" priority={priority}/>:<div className="flex h-full flex-col items-center justify-center p-6 text-center"><p className="font-serif text-2xl text-bone">{art.alt}</p><p className="mt-3 text-xs uppercase tracking-[.18em] text-amber">Recommended {art.ratio}</p><p className="mt-2 text-sm text-muted">Suggested image type: {art.type}</p></div>}{art.caption&&<figcaption className="absolute bottom-0 left-0 bg-ink/75 px-3 py-2 text-xs text-muted">{art.caption}</figcaption>}</figure>}
